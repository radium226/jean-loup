[project]
name = "jean-loup-pisugar"
version = "0.1.0"
dependencies = [
    "click>=8.1.8",
    "parsedatetime>=2.6",
    "pendulum>=3.0.0",
    "retrying>=1.3.4",
    "smbus>=1.1.post2",
]

[tool.uv]
package = true

[dependency-groups]
dev = []


[project.scripts]
jean-loup-pisugar = "jean_loup.pisugar.cli:app"
